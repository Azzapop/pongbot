# Sequel Database Configuration

development:
  adapter: mysql2
  database: pongbot_development
  username: root
  password:
  pool: 5
  timeout: 5000
  encoding: utf8
production:
  url: <%= ENV['CLEARDB_DATABASE_URL'] %>
